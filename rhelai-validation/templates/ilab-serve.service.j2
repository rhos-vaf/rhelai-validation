[Unit]
Description=ilab model serve service

[Install]
WantedBy=multi-user.target default.target

[Service]
ExecStart=ilab model serve --model-path {{[model_path_base | model_name] | join('/') | quote}}
Restart=always